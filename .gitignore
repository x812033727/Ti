# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
# pytest-cov 覆蓋率資料檔（CI 以 --cov 產生），屬執行期 artifact，不進版控。
.coverage
.coverage.*
.venv/
venv/
# QA / 驗證流程產生的暫存 venv 與 pre-commit 快取（本機 artifact，不進版控）
.qa-venv/
.qa_venv/
.pc-cache-qa/
# test_qa_no_bare_pytest.py 的 grep-fallback 測試會即時建立此 binstub 目錄（symlink），
# 屬執行期 artifact，不得進版控。
.qa-tmp-nopath/

# Secrets
.env
.env.*
!.env.example

# Generated agent workspaces
workspaces/

# Autopilot 執行期狀態目錄（AUTOPILOT_STATE_DIR，預設 PROJECT_ROOT/autopilot）
# 含 deploy.lock / backlog.json / backlog.lock 等運維狀態，不得進版控。
autopilot/

# Session history archive
history/

# 專案（長期產品）執行期狀態：meta + 專屬改良 backlog（TI_PROJECTS_ROOT，預設 PROJECT_ROOT/projects）
projects/

# 跨場次教訓庫（LESSONS_FILE，預設 PROJECT_ROOT/lessons.json）+ 其鎖檔，屬執行期狀態，不進版控。
lessons.json
lessons.lock
lessons.json.tmp

# ADR 的 flock 鎖檔（adr._locked 需要時自動重建），屬執行期狀態，不進版控；
# DECISIONS.md 與 adr.json 為刻意落盤的決策記錄，照常進版控。
adr.lock

# 沙箱 ro-bind 測試的 host 探針檔（test_qa_ro_bind_reject_qa.py，正常即時清理；防誤 commit）
.ti_ro_bind_probe_*.txt
