# 已知限制（Known Limitations）

本次以「核心可用、帶已知限制」版本交付；以下項目尚未滿足,已留待後續改良:

- [ ] 逐項核對 `lane_baseline_injection_inventory.md` 的 LaneContext 欄位、13+ 列對照表與每個 marker，是否仍相符現行 `orchestrator.py`/`runner.py`/`providers.py`/`experts.py`，記錄任何漂移點
